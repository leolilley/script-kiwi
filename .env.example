# Supabase (Script Kiwi DB)
SCRIPT_KIWI_SUPABASE_URL=https://your-project.supabase.co
SCRIPT_KIWI_SUPABASE_KEY=your-anon-key
SCRIPT_KIWI_SUPABASE_SERVICE_KEY=your-service-role-key

# =============================================================================
# PROXY CONFIGURATION
# =============================================================================
# See docs/proxy-setup.md for full documentation
#
# Three options:
#   1. Static URL:     Set PROXY_URL
#   2. Service creds:  Set PROXY_SERVICE + PROXY_USERNAME + PROXY_PASSWORD
#   3. Multi-proxy:    Set PROXY_PASSWORD + add usernames to .ai/tmp/nodemaven_proxies.txt
#
# Option 3 is recommended for rotation through multiple proxies.

# -----------------------------------------------------------------------------
# Option 1: Static proxy URL (simplest - single proxy)
# -----------------------------------------------------------------------------
# PROXY_URL=http://username:password@proxy.example.com:8080

# -----------------------------------------------------------------------------
# Option 2: Proxy service with dynamic URL building
# -----------------------------------------------------------------------------
# Supported: nodemaven, brightdata, oxylabs, smartproxy, decodo, webshare, scraperapi
#
# PROXY_SERVICE=nodemaven
# PROXY_USERNAME=your-username
# PROXY_PASSWORD=your-password
# PROXY_COUNTRY=us              # Optional: us, gb, nz, any
# PROXY_FILTER=medium           # Optional: medium, high (NodeMaven IP quality)
# PROXY_SESSION_ID=session123   # Optional: for sticky sessions
# PROXY_ZONE=residential        # Optional: Bright Data zone

# -----------------------------------------------------------------------------
# Option 3: Multi-proxy rotation from file (RECOMMENDED)
# -----------------------------------------------------------------------------
# For NodeMaven: paste usernames into .ai/tmp/nodemaven_proxies.txt (one per line)
# The ProxyPool will auto-load and rotate through all proxies.
#
# Example file content:
#   username-country-us-sid-abc123-ttl-24h-filter-medium
#   username-country-us-sid-def456-ttl-24h-filter-medium
#
PROXY_PASSWORD=your-nodemaven-password
# PROXY_FILE=.ai/tmp/nodemaven_proxies.txt   # Default location, optional to set

# =============================================================================
# External APIs
# =============================================================================
APIFY_API_TOKEN=your-apify-token
ANYMAILFINDER_API_KEY=your-anymailfinder-key
LEAD_MAGIC_API_KEY=your-leadmagic-key

# OpenAI (for embeddings if needed)
OPENAI_API_KEY=your-openai-key

# =============================================================================
# Server
# =============================================================================
SERVER_NAME=script-kiwi
LOG_LEVEL=INFO
